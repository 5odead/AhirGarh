## Packages
framer-motion | Page transitions and complex animations for a premium feel
fuse.js | Fast, lightweight fuzzy-search for the global SearchModal

## Notes
- No backend required; pure static React application
- Uses CSS variables for custom Ahirgarh colors to map to Tailwind
- Uses wouter for routing
- All data is hardcoded in client/src/data/
